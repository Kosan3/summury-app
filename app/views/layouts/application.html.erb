<!DOCTYPE html>
<html>
  <head>
    <title><%= full_title(yield(:title)) %></title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header style="background-color: #ff6633; padding-bottom: 15px;">
      <div class="container">
        <h2><%= link_to 'サイタマブログ', root_path, style: 'color: white; text-decoration: none; font-weight: bold;' %></h2>
        <ul class="nav nav-tabs nav-justified">
        <% if logged_in? %>
          <li class="active"><%= link_to '登録者情報', users_path %></li>
          <li class='active'><%= link_to 'マイページ', user_path(current_user) %></li>
          <li class="active"><%= link_to 'ログアウト', logout_path, method: :delete %></li>
        <% else %>
          <li class="active"><%= link_to 'HOME', root_path %></li>
          <li class="active"><%= link_to 'ABOUT', about_path %></li>
          <li class="active"><%= link_to 'LOGIN', login_path %></li>
        <% end %>
        </ul>
      </div>
    </header>
    <div class="container">
      <% flash.each do |type, message| %>
        <div class="alert alert-<%= type %>">
          <%= message %>
        </div>
      <% end %>
      <%= yield %>
    </div>
    <footer style="background-color: #ff6633; height: 50px;">
      <div class="container-fluid">
        <p style="text-align: center; color: white; line-height: 50px;">yuya.kosaka</p>
      </div>
    </footer>
  </body>
</html>
